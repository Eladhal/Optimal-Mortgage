import { SumValidatorDirective } from './sum-validator.directive';

describe('SumValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SumValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
